// services/api-gateway/src/api-gateway.smoke.test.ts

describe('api-gateway smoke test', () => {
  it('runs the Jest harness for api-gateway', () => {
    // This is intentionally minimal for now – we just want at least one test
    // in this package so Jest always has something to execute.
    expect(true).toBe(true);
  });
});