# Square POS Adapter

Tiny Python example that leverages the async SDK to register webhooks before a POS migration. Replace the placeholder URL and API key before running it.

```bash
pip install -e packages/sdk-python
python examples/pos-square/adapter.py
```
