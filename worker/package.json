{
  "name": "@apgms/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo build worker",
    "typecheck": "echo no typecheck for worker",
    "test": "node --test"
  },
  "dependencies": {
    "@apgms/shared": "workspace:*"
  }
}
