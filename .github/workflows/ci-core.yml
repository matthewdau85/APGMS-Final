name: CI Core - domain-policy tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ '**' ]

jobs:
  test-domain-policy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
=======
      - uses: actions/setup-node@v4
        with:
          node-version: '22.12.x'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
>>>>>>> 6716291060955e73728760cd8f3994d6c4cce83a

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run domain-policy tests
        run: pnpm -r --filter @apgms/domain-policy test
