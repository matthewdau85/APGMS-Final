[pytest]
testpaths = tests
addopts = -q
norecursedirs = .venv build dist node_modules .* Windows ProgramData
pythonpath = C:\loat-poc
# Optional: ignore heavy external data mirrors
norecursedirs = .venv build dist node_modules .* Windows ProgramData data\external\raw
# Optional: ignore heavy external data mirrors
norecursedirs = .venv build dist node_modules .* Windows ProgramData data\external\raw
