{
  "name": "@apgms/regwatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APGMS ATO change watcher (file-backed snapshots + small API).",
  "license": "UNLICENSED",
  "scripts": {
    "once": "tsx src/run-once.ts",
    "dev": "tsx watch src/server.ts",
    "serve": "node dist/server.js",
    "build": "tsc -p tsconfig.json && cp -r data dist/data || true",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@fastify/cors": "*",
    "@fastify/helmet": "*",
    "cheerio": "^1.2.0",
    "diff": "^8.0.3",
    "dotenv": "17.2.3",
    "execa": "^9.6.1",
    "fastify": "^5.7.2",
    "p-limit": "^7.2.0",
    "undici": "^7.19.1"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "@types/node": "^25.0.10",
    "rimraf": "^6.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
