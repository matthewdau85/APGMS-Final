# ml-core

The `ml-core` package contains feature engineering utilities, baseline models, and
experiment tracking helpers for APGMS risk analytics.

## Getting started

```bash
uv sync
uv run python -m ml_core.cli.build_features --help
```

Or with Poetry:

```bash
poetry install
poetry run python -m ml_core.cli.build_features --help
```
