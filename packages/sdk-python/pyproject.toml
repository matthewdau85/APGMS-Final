[project]
name = "apgms-sdk"
version = "0.1.0"
description = "Python SDK for the APGMS onboarding API"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]

[project.optional-dependencies]
test = ["pytest>=8.3.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
