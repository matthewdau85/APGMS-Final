{
  "name": "@apgms/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "generate:prisma": "pnpm exec prisma generate --schema prisma/schema.prisma",
    "prebuild": "pnpm run generate:prisma",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "otplib": "^12.0.1"
  }
}
